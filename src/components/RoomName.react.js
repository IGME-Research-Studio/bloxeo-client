const React = require('react');

const RoomName = React.createClass({
  /**
   * @return {object}
   */
  render: function() {
    return (
      <div>
        <h2>Room Name</h2>
      </div>
    );
  },
});

module.exports = RoomName;
