const React = require('react');

const SidebarRoomName = React.createClass({
  /**
   * @return {object}
   */
  render: function() {
    return (
      <div id="roomName">
        <h2>Room Name</h2>
      </div>
    );
  },
});

module.exports = SidebarRoomName;
